export const enum RoundsMen {
  KNOCK_OUT_ROUND = 1,
  FINALS = 2,
}

export const enum RoundsWomen {
  FINALS = 1,
}

export enum SLSPublicStatsSortColumns {
  ATHLETE = "athlete",
  ROUND_SCORE = "avgRoundScore",
  RUN_SCORE = "averageRunScore",
  TRICK_SCORE = "averageTrickScore",
  COMPLETION_PERCENT = "trickCompletionRate",
  MAX_ROUND_SCORE = "maxRoundScore",
  MIN_ROUND_SCORE = "minRoundScore",
}
