export enum TASK_TYPE {
  SCHEDULE = 1,
  ODDS_PUBLISH = 2,
  RESULTS_PUBLISH = 3,
}

export enum NOTIFICATION_TYPE {
  SCHEDULE = "schedule",
  ODDS_PUBLISH = "odds_publish",
  RESULTS_PUBLISH = "results_publish",
}
